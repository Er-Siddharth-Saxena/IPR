export interface loginResponse {
    error : boolean;
    message : string;
  }

export interface registerResponse {
    error : boolean;
    message : string;
}

export interface forgetPasswordResponse {
    error : boolean;
    message : string;
}

export interface updatePasswordResponse {
    error : boolean;
    message : string;
}

export interface getFaqsPatentResponse {
    error : boolean;
    message : string;
}
 
export interface getFaqsCopyRightResponse {
    error : boolean;
    message : string;
}

export interface getFaqsTrademarkResponse {
    error : boolean;
    message : string;
}

export interface getFaqsTradeSecretsResponse {
    error : boolean;
    message : string;
}

export interface positionsResponse {
    error : boolean;
    message : string;
}

export interface nearestResponse {
    error : boolean;
    message : string;
}